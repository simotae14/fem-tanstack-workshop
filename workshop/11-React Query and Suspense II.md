# React Query and Suspense

There's one place where transitions won't work.

Anything dealing with routing, route invalidations, etc. This is a limitation of React, currently.

This would fix that if it ever lands.

https://github.com/facebook/react/pull/35449

But fear not, we can easily work around it currently
