# Server Routes & More Middleware

Server Routes, which you might normally know as API routes.

They run only on the server.

Yes, just like server functions.

BUT they have a stable, callable url, based on the same file-system based routing rules we've already been using (and loving).

And yes, they can have middleware.

And while we're here, let's look at global middleware.
